// URL base para la API. Se puede sobreescribir con la variable de entorno VITE_API_BASE
export const API_BASE = import.meta.env.VITE_API_BASE || 'http://localhost:3000'

// export otras constantes relacionadas con el backend si fuera necesario
